{
  "scripts": {
    "dev": "concurrently --kill-others \"npx serve\" \"npx json-server db.json -p 3001\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "json-server": "^0.17.3",
    "serve": "^14.2.0"
  }
}
